@model SE2_IMDB.Models.ViewModels.Film.FilmList

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Films[0].Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Films[0].ReleaseYear)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Films[0].Popularity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Films[0].StoryLine)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Films) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReleaseYear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Popularity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StoryLine)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })*@
        </td>
    </tr>
}

</table>
